#!/bin/bash
# Скрипт для вычисления суммы двух чисел (с ошибкой)

if [ "$#" -ne 2 ]; then
    echo "Использование: \$0 число1 число2"
    exit 1
fi

# Ошибка: неверное вычисление суммы (умножение вместо сложения)
sum=$(( $1 + $2 ))
echo "Сумма: $sum"

